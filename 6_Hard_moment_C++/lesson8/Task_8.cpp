#include"Program.h"
#include <gtest/gtest.h>

TEST(sortPhone, sortByPhone)
{
    ASSERT_NO_THROW(&PhoneBook::SortByPhone);
}

TEST(sortName, sortByName)
{
    ASSERT_ANY_THROW(&PhoneBook::SortByName);
}

TEST(sortName2, sortByName2)
{
    ASSERT_NO_THROW(&PhoneBook::SortByName);
}

int main()
{
    ifstream file;
    file.open("PhoneBook.txt");
    PhoneBook book(file);
    file.close();

    testing::InitGoogleTest();
    return RUN_ALL_TESTS();
}